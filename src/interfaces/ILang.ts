export interface ILanguage {
  header: {
    search: string
    language: string
    fullScreen: string
    message: string
    notifications: string
    settings: string
  }
  slide: {
    dash: {
      dashboard: string
      statistic: string
    }
    menu: {
      menu: string
      users: string
      employees: string
      restaurants: string
      providers: string
      deliveries: string
    }
    notifications: {
      notifications: string
      messages: string
    }
  }
}
